# Namespace: oci-devops applications
# resource "oci_artifacts_container_repository" "udp-health-check-server-1700_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "udp-health-check-server-1700_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "udp-health-check-server-1700_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "udp-health-check-server-1700_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "udp-health-check-server-1710_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "udp-health-check-server-1710_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "udp-health-check-server-1710_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "udp-health-check-server-1710_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# Namespace: adailsilva-apps applications
# resource "oci_artifacts_container_repository" "spring-boot-app_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "spring-boot-app_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "spring-boot-app_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "spring-boot-app_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "homepage-80_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "homepage-80_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "homepage-80_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "homepage-80_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "api-k8s-dashboard-backend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "api-k8s-dashboard-backend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "api-k8s-dashboard-backend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "api-k8s-dashboard-backend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "app-k8s-dashboard-frontend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "app-k8s-dashboard-frontend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "app-k8s-dashboard-frontend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "app-k8s-dashboard-frontend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "api-chirpstack-consumer-backend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "api-chirpstack-consumer-backend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "api-chirpstack-consumer-backend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "api-chirpstack-consumer-backend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "app-chirpstack-consumer-frontend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "app-chirpstack-consumer-frontend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "app-chirpstack-consumer-frontend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "app-chirpstack-consumer-frontend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "api-cardscontrol-backend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "api-cardscontrol-backend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "api-cslab-cardscontrol-backend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "api-cslab-cardscontrol-backend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "app-cslab-cardscontrol-frontend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "app-cslab-cardscontrol-frontend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "app-cslab-cardscontrol-frontend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "app-cslab-cardscontrol-frontend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "api-cslab-web-backend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "api-cslab-web-backend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "api-cslab-web-backend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "api-cslab-web-backend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "app-cslab-web-frontend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "app-cslab-web-frontend_platform_linux--x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "app-cslab-web-frontend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "app-cslab-web-frontend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "api-stockcontrol-backend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "api-stockcontrol-backend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "api-stockcontrol-backend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "api-stockcontrol-backend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}

# resource "oci_artifacts_container_repository" "app-stockcontrol-frontend_platform_linux-x86_64" {
#   # Required
#   compartment_id = var.compartment_id
#   display_name   = "app-stockcontrol-frontend_platform_linux-x86_64"

#   # Optional
#   # defined_tags  = { "K8S.Apps" = "Free" }
#   # freeform_tags = { "K8S" = "Free Apps" }
#   is_immutable = false
#   is_public    = false
#   readme {
#     # Required
#     content = "Image repository with _**x86_64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
#     format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
#   }

# }

resource "oci_artifacts_container_repository" "app-stockcontrol-frontend_platform_linux-arm64" {
  # Required
  compartment_id = var.compartment_id
  display_name   = "app-stockcontrol-frontend_platform_linux-arm64"

  # Optional
  # defined_tags  = { "K8S.Apps" = "Free" }
  # freeform_tags = { "K8S" = "Free Apps" }
  is_immutable = false
  is_public    = false
  readme {
    # Required
    content = "Image repository with _**arm64 architecture**_ for k8s-on-arm-oci-always-free Cluster."
    format  = "text/markdown" #"text/plain" # Supported formats are text/plain and text/markdown.
  }

}
